version: '3.8'

services:
  velvet-bot:
    build: .
    container_name: velvet-bot
    restart: unless-stopped
    volumes:
      - ./config.json:/app/config.json:ro
      - ./bot.db:/app/bot.db

volumes:
  bot-data:
    driver: local
